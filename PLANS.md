# 実行計画

このファイルはCodexが継続的に更新する実装計画の正本です。完了項目、判断、検証結果、未解決事項を残してください。

## Hard Gate

`docs/POC_RESULT.md`の最終判定が`GO`になるまで、Milestone 1以降へ進んではならない。

- `GO WITH CHANGES`は未完了扱い。設計変更と再PoCが必要。
- 必須項目の「未確認」は失敗と同じ扱い。
- ヘルプ確認、コードのコンパイル、机上推測だけではゲートを通過しない。
- PoC中のコードは`poc/`へ隔離し、製品コードへ流用しない。

## Milestone 0: 実行可能性PoCとGO/NO-GO判定

目的: HandBrakeCLI統合とタイトルメタデータ後処理の根本仮定を実エンコードで検証し、製品実装を開始してよい目処を立てる。

手順:

1. ローカル環境と利用可能ツールを確認する。
2. 短く決定的な検証用MKVを生成または用意する。
3. `docs/POC_GATE.md`の必須試験をすべて実行する。
4. 結果をffprobe、映像・音声ハッシュ、終了コード、プロセス確認、VLC表示、ログで機械的に検証する。
5. 制約が見つかった場合は設計を修正し、影響項目を再試験する。
6. `docs/POC_RESULT.md`へ最終判定を書く。

成果物:

- `docs/LOCAL_INVESTIGATION.md`
- `docs/POC_RESULT.md`
- 必要に応じた設計書の修正
- `docs/DECISIONS.md`への判断記録
- `poc/`以下の再現可能な検証スクリプト、最小Goスパイク、検証ログの参照

必須完了条件:

- チャプター番号範囲だけで、HandBrake GUIと同等に先頭以外を含む指定区間をエンコードできる。
- GUIでも発生する境界重複・欠落を記録し、自動補正を加えない。
- 入力音声トラック1/2の番号対応と、1のみ・2のみ・1+2からChapterBrake定義の
  高品質・標準品質を生成できる。
- 高品質パススルー非対応時に、AACフォールバックを明示して欠落を防げる。
- MKVソフト字幕が格納され、映像へ焼き付いていないことをフレーム比較で確認できる。
- MP4字幕なしで焼き付けが発生しない。
- MKVをmkvpropeditで再多重化せずtitle変更でき、必要なストリーム・チャプター・内容が維持される。
- MP4をffmpegのストリームコピーでtitle変更でき、再エンコード、品質変化、音ズレ、ストリーム欠落がない。
- MKV/MP4をVLCで直接開き、出力ファイル名のstemとして表示される。
- タイトル設定・検証後だけ最終ファイル名へrenameする実装経路が確定している。
- プリセット一覧、GUIカスタムプリセット、コンテナ判定の実装経路が確定している。
- GoからHandBrakeCLIを即時停止でき、子プロセス残留がなく、終了後に部分出力を削除できる。
- stdout/stderrの完全保存と進捗表示の実装経路が確定している。
- 未解決の必須項目がない。
- `docs/POC_RESULT.md`が`GO`になっている。

状態: GO（2026-07-26、Milestone 1開始可能）

## Milestone 1: プロジェクト基盤と純粋ロジック

開始条件: Milestone 0が`GO`で完了していること。

目的: TUIや実コマンド実行に依存しないモデルと変換ロジックを実装する。

対象:

- Goモジュールと最小パッケージ構成
- 設定・キューの型、検証、原子的読み書き
- 出力名と次回インデックス計算
- チャプター開始位置の選択からエンコード範囲を生成する処理
- 23分40秒近似の初期選択ロジック
- 音声・字幕選択モデル
- PoCで確定したHandBrakeCLI、mkvpropedit、ffmpeg、ffprobe引数を構造化データから生成する処理
- 出力stemからのタイトル生成
- コンテナ別一時ファイル遷移
- 単体テスト

完了条件:

- 実HandBrakeCLIなしで全主要ロジックをテストできる。
- `gofmt`、`go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./...`が成功する。

状態: 完了（2026-07-26）

## Milestone 2: 外部コマンド統合とキューランナー

目的: メディア解析、プリセット取得、キュー永続化、HandBrakeCLI逐次実行、コンテナ別タイトル設定、即時中断、ログを実装する。

対象:

- PoCで確定した解析方法
- HandBrakeプリセット取得とコンテナ判定
- キュー先頭の逐次実行
- MKVのmkvpropeditタイトル設定と検証
- MP4のffmpegストリームコピーによるタイトル設定と検証
- 一時出力から最終名へのrename
- 成功時のみ先頭ジョブ削除
- エラー時の停止
- 即時中断と部分出力削除
- 既存出力の無条件削除・上書き
- アプリログとジョブ別のHandBrakeCLI・mkvpropedit・ffmpeg・ffprobe生ログ
- コマンド境界の統合テスト

状態: 完了（2026-07-26）

## Milestone 3: 最小TUI

目的: `docs/TUI_FLOW.md`に記載された画面だけを実装する。

対象:

- メインメニュー
- 簡素なMKVファイル選択
- プリセット選択
- 出力ベース名と開始インデックス
- チャプター開始位置の複数選択と経過時間再計算
- 音声トラック1・2の選択と高品質・標準品質の表示
- MKV字幕選択
- 実行プレビュー
- キュー追加
- キュー実行画面と即時中断

対象外:

- ファイル検索、ソート、隠しファイル切替
- キュー編集・並び替えUI
- 設定編集UI
- 複数同時エンコード
- 任意の非連続チャプター範囲編集

状態: 完了（2026-07-26）

## Milestone 4: 実ファイル受け入れ試験

目的: 小さなテストMKVと実利用ファイルで`docs/ACCEPTANCE_TESTS.md`を確認する。

注意: Milestone 0は統合方式の成立確認、Milestone 4は完成アプリの受け入れ確認であり、代替関係ではない。

成果物:

- 実施結果
- 使用したHandBrakeCLI、mkvpropedit、ffmpeg、ffprobe引数
- ログ例
- 発見した制約
- READMEの利用手順

状態: 完了（2026-07-26）

## Progress Log

Codexは作業日、実施内容、検証結果、残課題をここへ追記する。

### 2026-07-26 Milestone 0

- 初期仕様一式をローカルコミット`22643de`として保存した。
- HandBrakeCLI 1.11.2、FFmpeg/ffprobe 8.1.2、MKVToolNix 100.0、Go 1.26.5を実機確認した。
- 4チャプター、音声2、字幕2の再生成可能なMKVを作成し、P-01からP-10を実行した。
- GUIカスタムプリセット取込、JSONの`FileFormat`によるコンテナ判定を確認した。
- 音声1=640、音声2=640、音声1+2=640/160を実出力で確認した。
- MKVソフト字幕0/1/2とMP4字幕なしでデコード映像ハッシュが一致し、非焼き付けを確認した。
- MKV title直接編集、MP4ストリームコピーtitle設定、検証後renameを確認した。
- 2.18GBの実利用MKVでもmkvpropeditが0.07秒で完了し、内容と構造を維持した。
- HandBrakeCLI、ffmpeg、mkvpropeditのプロセスグループ中断、Wait後削除、同一ジョブ再実行を確認した。
- tview v0.42.0スパイクのtest/race/vet/buildが成功した。
- P-03で非先頭チャプターの開始前30フレーム混入を検出し、当初は不合格とした。
- HandBrake GUI 1.11.2を直接操作し、30秒チャプターのチャプター2だけをエンコードした。GUIでも先頭30フレームの混入、次チャプター先頭1フレームの混入、選択区間内1フレームの欠落を確認した。
- GUIと同等の境界挙動を許容し、自動補正しない方針を利用者判断で確定した。
- 完成MKV/MP4をVLCで直接開き、日本語・空白・記号を含むファイル名stemが表示タイトルになることを確認した。
- 音声ルールをプリセット順に適用すると640 kbps AACが320へ変更され、160 kbps AC-3パススルーが640のままになることを確認した。640/160をプリセット内の同ビットレートルールへ対応付ける経路は成立したが、音声UIの意味は未確定。
- 任意ビットレート入力を廃止し、各選択入力からプリセット由来の高品質・標準品質を
  生成する方式へ変更した。入力1/2/1+2、パススルー非対応時の明示フォールバック、
  MP4プリセットを実エンコードし、すべて合格した。
- プリセット選択はChapterBrake内蔵の`My Presets相当`を既定一覧へ表示し、
  HandBrake標準プリセットは「その他のプリセットから選ぶ」から別一覧へ入る
  二段構成とした。内蔵候補は`1080p MP4`、`1080p MKV`、`480p MP4`とし、
  HandBrake GUIやGUI設定ファイルへ依存しない。
- 音声方針も映像プリセットから分離し、入力を安全にパススルーできる場合は
  高品質側で使用し、非対応時はAAC 640 kbps・5.1chへフォールバックする。
  標準品質側はAAC 160 kbps・stereoとする。数値はUIへ出さない。
- `docs/POC_RESULT.md`を`GO`とし、Milestone 1以降の実装開始を許可する。

### 2026-07-26 Milestone 1

- 製品実装前にPoC・確定仕様をローカルコミット`d69a634`として保存した。
- ルートGoモジュールを作成し、PoCは独立した`poc/go.mod`配下へ隔離した。
- `settings.json`と`queue.json`の型、厳格なJSON読込、検証、fsyncとrenameを使う
  原子的保存を実装した。不正JSON・未知フィールド・未知versionは上書きしない。
- チャプター開始選択からの範囲生成、23分40秒近似、選択位置からの経過時間、
  Unicode対応の出力名、既存キュー・ファイル名からの次番号計算を実装した。
- GUI非依存の内蔵プリセット3件と、標準プリセットのコンテナ解析を実装した。
- 各入力音声から高品質・標準品質を作る音声計画を実装した。AC-3は高品質側で
  パススルーし、その他は高品質AACへフォールバックする。
- チャプター番号だけを使い字幕焼き付けを明示的に無効化するHandBrakeCLI引数、
  mkvpropedit、MP4用ffmpeg、ffprobe引数を実装した。
- MKV/MP4別の一時ファイル、title対象、検証後の公開元を実装した。
- `gofmt -w .`、`go test ./...`、`go test -race ./...`、`go vet ./...`、
  `go build ./...`、`git diff --check`が成功した。

### 2026-07-26 Milestone 2

- HandBrake JSONスキャンから再生時間、チャプター開始位置、HandBrake音声・字幕
  トラック番号を取得する実装とテストを追加した。
- 内蔵プリセットは外部参照なしで返し、「その他」はGUIを参照しない
  HandBrake標準プリセット一覧と選択時エクスポートJSONから解決するようにした。
- 外部コマンドをシェルなし・新規プロセスグループで起動し、キャンセル時は
  SIGINT、3秒後SIGKILL、Waitの順で停止する実装を追加した。
- キュー先頭を保持したままスキャン、エンコード、title設定、ffprobe検証、
  同一ディレクトリrenameを行い、成功後だけ原子的に先頭を削除するランナーを
  実装した。失敗・中断時は後続を実行せず、最終・一時出力を削除する。
- アプリログ、ジョブ要約ログ、各コマンドのstdout/stderr完全保存用サイドカー、
  HandBrake JSON進捗のストリーム解析を実装した。
- 偽コマンドでMKV/MP4成功、既存出力削除、失敗停止、中断、検証失敗、
  queue保存失敗をテストした。
- `CHAPTERBRAKE_INTEGRATION=1`の明示的統合試験を追加し、PoC fixtureを使って
  実HandBrakeCLI、ffmpeg、ffprobe、mkvpropeditによるMKV/MP4両方のランナー処理、
  title、キュー削除を確認した。
- 初回MP4統合試験では、チャプター用`bin_data`のlanguageタグが`und`から`eng`へ
  変わるPoC既知の差を構造破壊として過剰検出した。dataストリームのlanguageだけを
  比較対象外にし、音声・字幕のlanguage比較は維持した。再試験は両形式で成功した。
- `gofmt -w .`、`go test ./...`、`go test -race ./...`、`go vet ./...`、
  `go build ./...`、`git diff --check`が成功した。

### 2026-07-26 Milestone 3

- `cmd/chapterbrake`を追加し、初回起動時の設定・キュー・ログ初期化とtview TUIを
  組み立てた。
- 起動ディレクトリから、OS列挙順のディレクトリと通常MKVだけを表示する
  ファイル選択を実装した。一覧段階ではメディア解析しない。
- GUI非依存の内蔵プリセット3件を既定表示し、「その他」からだけHandBrake標準
  プリセット一覧へ移動する二段構成を実装した。
- 出力base・開始番号、23分40秒近似と手動チャプター選択、入力音声1/2、
  MKV字幕、実行プレビュー、既存出力の明示承認、キュー追加を実装した。
- キュー表示は読み取り専用とし、編集・並べ替え機能は追加していない。
- キュー実行画面へジョブ位置、段階、HandBrake進捗、ETA、Ctrl+C即時中断を
  実装した。シミュレーション画面で中断後にTUIが固まらず、キュー先頭保持と
  部分出力削除後に操作へ戻ることをrace付きで確認した。
- `gofmt -w .`、`go test ./...`、`go test -race ./...`、`go vet ./...`、
  `go build ./...`、`git diff --check`が成功した。

### 2026-07-26 Milestone 4

- 完成アプリを初回起動し、`~/Documents/ChapterBrake/`の設定、空キュー、ログ作成、
  既定出力先、ファイルmodeを確認した。必須ツールの絶対パスとバージョンも
  アプリログへ記録された。
- PoCの短いfixtureを使い、完成ランナーからMKV/MP4を実HandBrakeCLIで再エンコードし、
  MKVはmkvpropedit、MP4はffmpeg stream copyでtitleを設定した。
- ffprobeで後処理前後の映像、音声、字幕、チャプター構造とtitleを検証し、
  成功後だけ最終名へ公開、キュー先頭削除となることを確認した。
- 完成MKV/MP4をVLCで直接開き、両形式の表示タイトルがファイル名stemの
  `統合 Title #01`になることを確認した。
- TUIのファイル、プリセット、出力名、チャプター、音声、字幕、プレビュー、キュー
  画面を直接組み立てる回帰テストを追加した。その過程で、字幕なしを選ぶと空配列が
  `nil`へ変わりジョブ検証に失敗する問題を検出し、空配列維持の実装修正と
  専用テストを追加した。
- `docs/ACCEPTANCE_RESULT.md`へAからLの結果、実コマンド、ログ、既知制約を記録した。
- `gofmt -w .`、`go test ./...`、`go test -race ./...`、`go vet ./...`、
  `go build ./...`、`git diff --check`、実ツール統合試験が成功した。
- `go list ./...`で`poc/`が製品モジュールに含まれないことを確認した。

### 2026-07-26 起動構成の整理

- `cmd/chapterbrake/main.go`を終了コード処理と`bootstrap.Run`呼び出しだけへ縮小した。
- 設定、キュー、ログ、ツール検査、ランナー、TUI、OSシグナルの配線を
  `internal/bootstrap`へ移した。
- 起動時の依存組み立て、エラー伝播、コンテキストキャンセル時のTUI停止を
  外部ツール不要のテストで確認できるようにした。

### 2026-07-26 インストール手順

- 既定の`make`でビルドし、`~/.local/bin/chapterbrake`へmode 0755で
  インストールするMakefileを追加した。
- `make build`、`make test`と、`PREFIX`または`BINDIR`による配置先上書きに
  対応した。

### 2026-07-26 ライセンスと除外規則

- コミット作者名を著作権者とする2026年のMIT License本文を`LICENSE`へ追加した。
- `.gitignore`を現在のMakefile、Goテスト、ChapterBrake一時出力、PoC生成物に
  合わせて整理し、現在使っていない規則を削除した。

### 2026-07-26 プリセット不具合修正

- 実HandBrakeCLI 1.11.2で`--preset-list`の一覧本体がstderrへ出ることを再現し、
  stdoutだけを解析していたため「その他のプリセット」でエラーになる原因を特定した。
- stdout/stderr両方を別々に保存・解析するよう修正し、実HandBrakeCLIによる
  一覧取得と`Fast 1080p30`の選択・MP4判定まで統合試験を追加した。
- インストール済みTUIを実操作し、「その他のプリセットから選ぶ」から
  HandBrake標準プリセット一覧へエラーなく遷移することを確認した。
- GUI実設定の4件を再比較し、`My Old Presets`は480p自動クロップ、
  `GCCX`は480pクロップなしであることを確認した。誤って3件へ統合していた
  内蔵一覧を4件へ戻し、crop modeを明示する実装とテストを追加した。
- 上下60pxの黒帯を持つ720x480検証入力を実エンコードし、autoは720x360、
  noneは720x480になることをffprobeで確認した。

### 2026-07-26 TUIキー操作改善

- 一覧とフォームの主操作を矢印キーへ統一し、既存のTab、Enter、Space、Esc操作も
  維持する。
- 一覧の右キーを決定、左キーを戻る操作とし、チェック項目は左右どちらでも
  切り替えられるようにする。
- 入力欄以外のBackspaceを戻る操作へ追加する。文字列・数値入力中は従来どおり
  カーソル移動と文字削除を優先する。
- 画面タイトルへ利用可能なキーを表示し、操作を発見しやすくする。
- 単体テストに全キー変換と入力欄例外を追加し、インストール済みTUIで一覧の
  右決定、フォームの上下移動、左右チェック、入力欄の文字削除、Backspaceで
  前画面へ戻る操作を確認した。

### 2026-07-26 開発開始用ファイルの整理

- 完成後は不要な`CODEX_START.md`、`prompts/`の段階別指示、
  `DESIGN_MANIFEST.json`、使用済みPoC結果テンプレートをGit管理対象から削除した。
- 今後の保守判断に使う`AGENTS.md`、本計画、確定仕様、PoCの実施結果は維持した。
- `.gitignore`へ削除対象の規則は追加していない。

### 2026-07-26 AGENTS.mdの保守向け整理

- 完了済みの「PoCがGOになるまで製品コードを作らない」という初回実装向け指示を
  削除し、PoC完了済みであることと再検証が必要な変更範囲を明記した。
- 仕様の正本と、判断・統合・PoC・受け入れ・進捗の補助資料を役割別に整理した。
- 継続する絶対条件、Go実装方針、テスト同時更新、検証コマンドは維持した。

### 2026-07-26 TUIのEnterとEsc操作

- チャプター、音声、字幕のチェック項目上でEnterを押した場合、チェックを変更せず
  各画面の「次へ」を実行するよう変更した。
- キュー追加フローでは、Esc一回でどの設定画面からでもメインメニューへ戻るよう
  統一した。Backspaceは一段戻り、入力欄では文字削除として維持した。
- 出力ベース名または開始番号の入力欄でも、Enter一回で入力検証後にチャプター画面へ
  進むよう統一した。

### 2026-07-26 入力ディレクトリの起動設定

- `settings.json`をversion 2へ更新し、既定値
  `/Volumes/2TB HDD/Images`の`input_directory`を追加した。
- 引数なしでは設定した入力先からファイル選択を開始し、`--cwd`付きでは
  設定を書き換えず、その一回だけカレントディレクトリから開始するようにした。
- version 1の既存設定は`output_directory`を維持したまま原子的に移行する。
- 単体・race・vet・build検証に加え、インストール済みバイナリで通常起動と
  `--cwd`起動の初期位置を実確認した。

### 2026-07-26 入力ディレクトリ指定オプション

- カレントディレクトリ専用の`--cwd`を、任意の一時入力先を指定できる
  `--directory PATH`と短縮形`-d PATH`へ置き換えた。
- 相対パスを絶対パスへ解決し、存在するディレクトリであることを検証する。
- インストール済みバイナリを`poc/artifacts`から`-d .`で起動し、同ディレクトリが
  初期位置になることと、設定が変更されないことを確認した。

### 2026-07-26 チャプター区切り時間設定

- `settings.json`をversion 3へ更新し、`分:秒`形式で既定値`23:40`の
  `chapter_interval`を追加した。
- version 1・2の既知設定を、既存入出力先を維持したまま原子的に移行するようにした。
- チャプター分割画面へ区切り時間入力を追加し、その回だけ変更可能にした。
  入力欄のEnterまたは近似選択ボタンで、新しい時間による開始位置を選択し直す。
- 解析、設定移行、サービス初期選択、TUIの変更・エラー経路をテストした。
- インストール済みTUIで`23:40`から`45:00`への変更と再計算表示を確認し、
  終了後も実設定が`23:40`のままであることを確認した。

### 2026-07-26 ファイル選択の名前昇順化

- OSのディレクトリエントリ列挙順をそのまま表示する方式をやめ、`../`を先頭へ
  固定したうえで、残りを大文字小文字を区別しない名前昇順にする。
- ディレクトリとMKVを同じ名前順に並べ、同一視される名前は元文字列で決定的にする。
- 表示対象、除外、容量取得の既存挙動を維持し、順序を専用テストで固定する。

### 2026-07-26 実行中キュー操作・末尾除外・出力先更新

- 実行画面のEsc/Backspaceでエンコードを継続したままメインへ戻り、同じアプリ内で
  ジョブ追加と待機ジョブ削除を行えるようにする。実行中先頭は削除を拒否する。
- キューの追加、削除、先頭占有、完了を排他的な原子操作へ変更し、ジョブ完了ごとに
  再読込して実行中に追加されたジョブを失わず処理する。
- 単一起動lockを追加し、別プロセスからのキュー競合と複数同時エンコードを防ぐ。
- 実行中の終了操作は確認後に外部プロセス中断、一時出力削除を完了して終了する。
- 区切り時間入力欄のEnterは近似選択を更新した後、音声画面へ進む。
- 2秒以下の最終チャプターは末尾除外を初期オンにし、画面で解除可能にする。
- `settings.json`をversion 4へ更新し、出力先の新既定値を
  `/Volumes/2TB HDD/Movies`とする。version 3の旧既定値だけ新値へ移行する。
- 通常テスト、race、vet、build、diff checkをすべて通し、インストール済みTUIで
  version 4移行、新出力先、末尾除外表示、区切り時間Enter遷移、キュー削除表示を
  実確認した。実運用の既存キュー15件は変更していない。

### 2026-07-26 キュー統合・ジョブ境界一時停止・末尾結合

- 独立したキュー実行画面をキュー一覧へ統合し、実行中行へ段階、進捗、ETAを表示する。
- Enterのジョブ詳細に削除、実行・再開、現在ジョブ後の一時停止、
  即時中断して一時停止を集約する。
- 待機ジョブは`j`/`k`で一段ずつ並び替え、実行中先頭は固定する。
- キュー追加成功時に自動実行を開始する。一時停止中に追加した場合は停止を維持する。
- 通常終了は現在ジョブを完了した後、残りの永続キューを停止してアプリを終了する。
  即時中断は現在ジョブを先頭に残し、部分出力を削除して後続へ進まない。
- チャプター画面へ動画全体、絶対開始、単体時間、生成区間合計時間を表示する。
- 自動近似で短い末尾単体出力が生じる場合は、末尾を直前区間へ結合して先頭に通知する。
  実データ`くまクマ熊ベアーぱーんち！ 第3巻_t00.mkv`を再スキャンし、全長1:34:45、
  Chapter 21が16:31、期待開始位置が`1,7,13,19`、最終区間が約23:41であることを
  回帰テストへ固定した。
- 出力と隠し一時ファイルを`<output_directory>/<ベース名>/`へ置き、既存の同名
  ディレクトリを再利用する。

### 2026-07-28 キュー可視化・実エンコード一時停止・GUIプリセット取込

- キューへチャプター範囲由来の参考動画時間を保存し、メイン、キュー一覧、
  ジョブ詳細へ表示する。現在ジョブの実績速度と全残り動画時間から全体ETAを概算する。
- メインへ全キューの読み取り専用一覧を追加し、異常終了時はメインとキュー一覧の
  枠・タイトルを赤くする。`state.json`に実行中・失敗を原子的に記録し、
  実行中のまま再起動した場合も前回異常終了として表示する。
- HandBrakeCLIの専用プロセスグループへ`SIGSTOP` / `SIGCONT`を送り、
  同一アプリ起動中のエンコード一時停止・再開を追加する。ジョブ境界停止と
  即時中断も別操作として維持する。
- ジョブ追加後は自動実行を開始しつつ、同じ入力ディレクトリのファイル選択へ戻して
  連続登録できるようにする。
- 待機ジョブの`j` / `k`並び替え後も移動したジョブへカーソルを追従させ、
  同じキーを繰り返して連続移動できるようにする。
- チャプター画面を固定列の表へ変更し、選択、番号、開始、単体時間、出力合計、
  タイトルのヘッダと値を揃える。ファイル画面の左キー親移動を廃止した。
- `~/Documents/ChapterBrake/My Presets.json`のGUIエクスポートを読み、
  各キュージョブで`--preset-import-file`を使う。ファイルがない場合だけ
  互換内蔵4件を表示する。
- 新規ログを`~/Library/Logs/ChapterBrake`へ移し、タイトル名の出力子ディレクトリは
  キュー追加時ではなくジョブ開始時に作成する。
- 既存ログ417ファイル（96,985,854 bytes）を
  `~/Documents/ChapterBrake/logs`から`~/Library/Logs/ChapterBrake`へ移動し、
  移動前後の相対パス付きSHA-256集約値
  `079ab7166279b8a4354a8c7207d998eb9d8e63696677a655c73e1869bbc2a1c0`
  が一致することを確認した。旧ログディレクトリは残していない。
- `~/Downloads/My Presets.json`を通常データ場所へコピーし、コピー元・先の
  SHA-256が`cf2a457cc97c6d6a8b379841a4b7eb0a291e0395b2648bf5cd724dbfe887ed6b`
  で一致することを確認した。コピー元は保持した。
- 2026-07-27 11:58開始の指定ジョブログを調査した。HandBrakeは12:00:07に
  `av_interleaved_write_frame`の`Input/output error`でexit code 4となり、
  同時刻のmacOS統合ログでは`/Volumes/2TB HDD`を含む外部ディスク群が取り外され、
  約10秒後に再認識・再マウントされていた。再実行成功とも整合し、原因は
  一時的な外部ストレージ切断と判断した。

## 全作業完了時の権限クリーンアップ

全Milestoneと受け入れ確認が完了した最後に、今回の作業中に付与された権限を
すべて剥奪する。少なくともCodexのGUI操作、サンドボックス外の
`HandBrakeCLI`実行、VLC起動に関する永続承認を確認し、残さない。

## 全作業完了時のPoC整理

PoC固有のスクリプト、スパイク、再生成用fixtureは`poc/`内に隔離し、製品の
ビルド・テスト・実行時依存へ含めない。生成メディアと実行ログはGit管理しない。
全Milestone完了時に`poc/`外へPoC実体が混在していないことを再確認する。
成立根拠と設計判断は`docs/POC_RESULT.md`、`docs/LOCAL_INVESTIGATION.md`、
`docs/DECISIONS.md`へ残す。
