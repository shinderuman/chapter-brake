# 実行計画

このファイルはCodexが継続的に更新する実装計画の正本です。完了項目、判断、検証結果、未解決事項を残してください。

## Hard Gate

`docs/POC_RESULT.md`の最終判定が`GO`になるまで、Milestone 1以降へ進んではならない。

- `GO WITH CHANGES`は未完了扱い。設計変更と再PoCが必要。
- 必須項目の「未確認」は失敗と同じ扱い。
- ヘルプ確認、コードのコンパイル、机上推測だけではゲートを通過しない。
- PoC中のコードは`poc/`へ隔離し、製品コードへ流用しない。

## Milestone 0: 実行可能性PoCとGO/NO-GO判定

目的: HandBrakeCLI統合とタイトルメタデータ後処理の根本仮定を実エンコードで検証し、製品実装を開始してよい目処を立てる。

手順:

1. ローカル環境と利用可能ツールを確認する。
2. 短く決定的な検証用MKVを生成または用意する。
3. `docs/POC_GATE.md`の必須試験をすべて実行する。
4. 結果をffprobe、映像・音声ハッシュ、終了コード、プロセス確認、VLC表示、ログで機械的に検証する。
5. 制約が見つかった場合は設計を修正し、影響項目を再試験する。
6. `docs/POC_RESULT.md`へ最終判定を書く。

成果物:

- `docs/LOCAL_INVESTIGATION.md`
- `docs/POC_RESULT.md`
- 必要に応じた設計書の修正
- `docs/DECISIONS.md`への判断記録
- `poc/`以下の再現可能な検証スクリプト、最小Goスパイク、検証ログの参照

必須完了条件:

- チャプター番号範囲だけで、HandBrake GUIと同等に先頭以外を含む指定区間をエンコードできる。
- GUIでも発生する境界重複・欠落を記録し、自動補正を加えない。
- 入力音声トラック1/2の番号対応と、1のみ・2のみ・1+2からChapterBrake定義の
  高品質・標準品質を生成できる。
- 高品質パススルー非対応時に、AACフォールバックを明示して欠落を防げる。
- MKVソフト字幕が格納され、映像へ焼き付いていないことをフレーム比較で確認できる。
- MP4字幕なしで焼き付けが発生しない。
- MKVをmkvpropeditで再多重化せずtitle変更でき、必要なストリーム・チャプター・内容が維持される。
- MP4をffmpegのストリームコピーでtitle変更でき、再エンコード、品質変化、音ズレ、ストリーム欠落がない。
- MKV/MP4をVLCで直接開き、出力ファイル名のstemとして表示される。
- タイトル設定・検証後だけ最終ファイル名へrenameする実装経路が確定している。
- プリセット一覧、GUIカスタムプリセット、コンテナ判定の実装経路が確定している。
- GoからHandBrakeCLIを即時停止でき、子プロセス残留がなく、終了後に部分出力を削除できる。
- stdout/stderrの完全保存と進捗表示の実装経路が確定している。
- 未解決の必須項目がない。
- `docs/POC_RESULT.md`が`GO`になっている。

状態: GO（2026-07-26、Milestone 1開始可能）

## Milestone 1: プロジェクト基盤と純粋ロジック

開始条件: Milestone 0が`GO`で完了していること。

目的: TUIや実コマンド実行に依存しないモデルと変換ロジックを実装する。

対象:

- Goモジュールと最小パッケージ構成
- 設定・キューの型、検証、原子的読み書き
- 出力名と次回インデックス計算
- チャプター開始位置の選択からエンコード範囲を生成する処理
- 23分40秒近似の初期選択ロジック
- 音声・字幕選択モデル
- PoCで確定したHandBrakeCLI、mkvpropedit、ffmpeg、ffprobe引数を構造化データから生成する処理
- 出力stemからのタイトル生成
- コンテナ別一時ファイル遷移
- 単体テスト

完了条件:

- 実HandBrakeCLIなしで全主要ロジックをテストできる。
- `gofmt`、`go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./...`が成功する。

状態: 未着手

## Milestone 2: 外部コマンド統合とキューランナー

目的: メディア解析、プリセット取得、キュー永続化、HandBrakeCLI逐次実行、コンテナ別タイトル設定、即時中断、ログを実装する。

対象:

- PoCで確定した解析方法
- HandBrakeプリセット取得とコンテナ判定
- キュー先頭の逐次実行
- MKVのmkvpropeditタイトル設定と検証
- MP4のffmpegストリームコピーによるタイトル設定と検証
- 一時出力から最終名へのrename
- 成功時のみ先頭ジョブ削除
- エラー時の停止
- 即時中断と部分出力削除
- 既存出力の無条件削除・上書き
- アプリログとジョブ別のHandBrakeCLI・mkvpropedit・ffmpeg・ffprobe生ログ
- コマンド境界の統合テスト

状態: 未着手

## Milestone 3: 最小TUI

目的: `docs/TUI_FLOW.md`に記載された画面だけを実装する。

対象:

- メインメニュー
- 簡素なMKVファイル選択
- プリセット選択
- 出力ベース名と開始インデックス
- チャプター開始位置の複数選択と経過時間再計算
- 音声トラック1・2の選択と高品質・標準品質の表示
- MKV字幕選択
- 実行プレビュー
- キュー追加
- キュー実行画面と即時中断

対象外:

- ファイル検索、ソート、隠しファイル切替
- キュー編集・並び替えUI
- 設定編集UI
- 複数同時エンコード
- 任意の非連続チャプター範囲編集

状態: 未着手

## Milestone 4: 実ファイル受け入れ試験

目的: 小さなテストMKVと実利用ファイルで`docs/ACCEPTANCE_TESTS.md`を確認する。

注意: Milestone 0は統合方式の成立確認、Milestone 4は完成アプリの受け入れ確認であり、代替関係ではない。

成果物:

- 実施結果
- 使用したHandBrakeCLI、mkvpropedit、ffmpeg、ffprobe引数
- ログ例
- 発見した制約
- READMEの利用手順

状態: 未着手

## Progress Log

Codexは作業日、実施内容、検証結果、残課題をここへ追記する。

### 2026-07-26 Milestone 0

- 初期仕様一式をローカルコミット`22643de`として保存した。
- HandBrakeCLI 1.11.2、FFmpeg/ffprobe 8.1.2、MKVToolNix 100.0、Go 1.26.5を実機確認した。
- 4チャプター、音声2、字幕2の再生成可能なMKVを作成し、P-01からP-10を実行した。
- GUIカスタムプリセット取込、JSONの`FileFormat`によるコンテナ判定を確認した。
- 音声1=640、音声2=640、音声1+2=640/160を実出力で確認した。
- MKVソフト字幕0/1/2とMP4字幕なしでデコード映像ハッシュが一致し、非焼き付けを確認した。
- MKV title直接編集、MP4ストリームコピーtitle設定、検証後renameを確認した。
- 2.18GBの実利用MKVでもmkvpropeditが0.07秒で完了し、内容と構造を維持した。
- HandBrakeCLI、ffmpeg、mkvpropeditのプロセスグループ中断、Wait後削除、同一ジョブ再実行を確認した。
- tview v0.42.0スパイクのtest/race/vet/buildが成功した。
- P-03で非先頭チャプターの開始前30フレーム混入を検出し、当初は不合格とした。
- HandBrake GUI 1.11.2を直接操作し、30秒チャプターのチャプター2だけをエンコードした。GUIでも先頭30フレームの混入、次チャプター先頭1フレームの混入、選択区間内1フレームの欠落を確認した。
- GUIと同等の境界挙動を許容し、自動補正しない方針を利用者判断で確定した。
- 完成MKV/MP4をVLCで直接開き、日本語・空白・記号を含むファイル名stemが表示タイトルになることを確認した。
- 音声ルールをプリセット順に適用すると640 kbps AACが320へ変更され、160 kbps AC-3パススルーが640のままになることを確認した。640/160をプリセット内の同ビットレートルールへ対応付ける経路は成立したが、音声UIの意味は未確定。
- 任意ビットレート入力を廃止し、各選択入力からプリセット由来の高品質・標準品質を
  生成する方式へ変更した。入力1/2/1+2、パススルー非対応時の明示フォールバック、
  MP4プリセットを実エンコードし、すべて合格した。
- プリセット選択はChapterBrake内蔵の`My Presets相当`を既定一覧へ表示し、
  HandBrake標準プリセットは「その他のプリセットから選ぶ」から別一覧へ入る
  二段構成とした。内蔵候補は`1080p MP4`、`1080p MKV`、`480p MP4`とし、
  HandBrake GUIやGUI設定ファイルへ依存しない。
- 音声方針も映像プリセットから分離し、入力を安全にパススルーできる場合は
  高品質側で使用し、非対応時はAAC 640 kbps・5.1chへフォールバックする。
  標準品質側はAAC 160 kbps・stereoとする。数値はUIへ出さない。
- `docs/POC_RESULT.md`を`GO`とし、Milestone 1以降の実装開始を許可する。

## 全作業完了時の権限クリーンアップ

全Milestoneと受け入れ確認が完了した最後に、今回の作業中に付与された権限を
すべて剥奪する。少なくともCodexのGUI操作、サンドボックス外の
`HandBrakeCLI`実行、VLC起動に関する永続承認を確認し、残さない。

## 全作業完了時のPoC整理

PoC固有のスクリプト、スパイク、再生成用fixtureは`poc/`内に隔離し、製品の
ビルド・テスト・実行時依存へ含めない。生成メディアと実行ログはGit管理しない。
全Milestone完了時に`poc/`外へPoC実体が混在していないことを再確認する。
成立根拠と設計判断は`docs/POC_RESULT.md`、`docs/LOCAL_INVESTIGATION.md`、
`docs/DECISIONS.md`へ残す。
