# AGENTS.md

## プロジェクト

ChapterBrakeは、macOS上でHandBrakeCLIを対話的に設定し、永続キューを順次実行する
GoバックエンドとHTML/CSS/JavaScriptによるローカルWebアプリケーションである。

## 仕様と記録

仕様の正本は次の順で扱う。

1. `docs/REQUIREMENTS.md`
2. `docs/DATA_FORMATS.md`
3. `docs/WEB_FLOW.md`
4. `docs/ARCHITECTURE.md`
5. `docs/ACCEPTANCE_TESTS.md`

補助資料の役割は次のとおり。

- `docs/DECISIONS.md`: 設計判断と理由
- `docs/HANDBRAKE_INTEGRATION.md`: 外部ツールとの統合方式
- `docs/WEB_API.md`: ChapterBrake WebバックエンドのHTTP/SSE契約
- `docs/WEB_UI.md`: Web画面仕様

文書間に矛盾がある場合は、推測で仕様を増やさず、矛盾を記録してから解消する。

## 作業ルール

- 作業前に対象範囲の仕様と関連する補助資料を読む。複数機能へまたがる変更では
  仕様の正本をすべて確認する。
- リポジトリルートに`AGENTS.local.md`がある場合は、Git管理外のローカル指示として
  先に読む。
- 実行可能な調査、テスト、ビルドはCodex自身が行い、利用者へ代行させない。
- HandBrakeCLI、ffprobe/ffmpeg、mkvpropedit、macOS、Chrome/Edgeの挙動を記憶だけで
  決めず、変更に関係する範囲をローカル環境で確認する。
- 要求されていない検索、ソート、設定画面、キュー編集などを追加しない。
- 変更範囲を小さく保ち、無関係なリファクタリングを混ぜない。
- テストがあるコードを変更する場合は、対応するテストも同じ作業単位で更新する。
- 設計判断は`docs/DECISIONS.md`へ、外部ツールの現行制約は
  `docs/HANDBRAKE_INTEGRATION.md`へ反映する。

## 外部ツール変更の扱い

次を変更する場合は、関連する統合試験を確認し、影響範囲を実ツールで再検証する。

- HandBrakeCLIのチャプター、プリセット、音声、字幕引数
- HandBrakeCLI、ffmpeg、mkvpropedit、ffprobeの実行・中断方式
- MKV/MP4のタイトル設定、構造検証、一時ファイル公開手順
- 外部ツールやWebブラウザ統合の更新

再検証結果と新しい制約は、該当する仕様、`docs/DECISIONS.md`、
`docs/HANDBRAKE_INTEGRATION.md`へ反映する。

## Go実装方針

- Go 1.26系の利用可能な最新パッチ版で検証する。
- 標準ライブラリを優先し、外部依存は明確な価値がある場合だけ追加する。
- `gofmt`に従い、単純な制御フロー、早期return、小さな関数を使う。
- パッケージ名は短く具体的にし、無関係な処理を`util`、`common`、`helpers`へ
  集めない。
- 公開APIは最小限にし、アプリ内部のコードは原則`internal/`以下へ置く。
- インターフェースは利用側で、境界分離に必要な最小メソッドだけ定義する。
- 不要なDIコンテナ、サービスロケータ、抽象ファクトリ、イベントバスを導入しない。
- `context.Context`はキャンセル可能な処理とアプリ境界で伝播させ、構造体へ保存しない。
- エラーは意味を付けて`%w`でラップし、通常の実行エラーで`panic`しない。
- 診断ログには`log/slog`を使い、利用者向け表示と混同しない。
- 外部コマンドはシェル文字列へ連結せず、`os/exec`へ引数配列で渡す。
- パスは`path/filepath`で扱い、空白・日本語を含むパスを保護する。
- JSONは型付き構造体で読み書きし、読み込み時に明示的に検証する。
- `queue.json`と`settings.json`は同一ディレクトリの一時ファイルから原子的に
  置換する。破損時はfail closedとし、推測で修復・上書きしない。
- 外部コマンド境界を差し替え可能に保ち、単体テストを実ツールへ依存させない。
- テーブル駆動テストと`testing.T.TempDir()`を基本にする。

## 検証

Goコードまたはビルド構成を変更した場合は、完了前に最低限次を実行する。

```sh
gofmt -w .
go test ./...
go test -race ./...
go vet ./...
go build ./...
```

文書だけの変更でも参照切れと`git diff --check`を確認する。外部ツール連携を
変更した場合は、関連する明示的統合試験と実コマンド検証も行う。

失敗した検証は放置せず、原因を修正するか、環境上の制約と出力を記録する。

## 絶対条件

- エンコード範囲はHandBrakeCLIのチャプター番号範囲で指定し、秒・ミリ秒・PTSへ
  変更しない。
- 字幕を焼き付けず、プリセット由来の自動焼き付けも明示的に無効化する。
- キュー先頭ジョブは成功するまで`queue.json`から削除しない。
- 即時中断時は実行中プロセスを停止し、処理中の出力を削除して先頭ジョブを残す。
- 実行時の既存出力は、キュー追加時に承認済みとして再確認せず削除・上書きする。
- HandBrakeCLIは最終出力名ではなく、出力ディレクトリ内のジョブ固有一時ファイルへ
  書き込む。
- DLNA表示用タイトルは最終出力ファイル名から拡張子を除いた値に固定する。
- MKVは`mkvpropedit`でタイトルだけを直接変更し、FFmpegで再多重化しない。
- MP4はFFmpegのストリームコピーでタイトルを設定し、再エンコードしない。
- タイトル設定と検証が完了するまで最終ファイル名へ公開しない。
- 不正な手編集JSONを自動修復しない。
