# AGENTS.md

## 目的

このリポジトリでは、macOS上でHandBrakeCLIを対話的に設定し、永続キューとして順次実行するGo製TUIアプリケーションを開発する。

仕様の正本は次の順で扱う。

1. `docs/REQUIREMENTS.md`
2. `docs/DATA_FORMATS.md`
3. `docs/TUI_FLOW.md`
4. `docs/ARCHITECTURE.md`
5. `docs/ACCEPTANCE_TESTS.md`
6. `PLANS.md`

文書間に矛盾がある場合は、実装を進めず矛盾を記録する。推測で仕様を増やさない。

## 開発時の必須行動

- 作業前に、このファイルと上記の仕様書をすべて読む。
- 複数段階の作業では`PLANS.md`を更新し、現在地・判断・検証結果を残す。
- 実行可能なコマンド、調査、テスト、ビルドはCodex自身が実行する。利用者に代行させない。
- HandBrakeCLI、ffprobe/ffmpeg、mkvpropedit、macOS、TUIライブラリの挙動を記憶だけで決めず、ローカル環境で実行して確認する。
- コードを書いただけで終了しない。各段階でフォーマット、ビルド、テスト、静的検査を実行する。
- 失敗した検証を放置しない。原因を直すか、環境上の制約を具体的な出力付きで記録する。
- 要求されていない機能を追加しない。便利そうという理由だけで検索、ソート、設定画面、キュー編集UIなどを増やさない。
- 変更範囲を小さく保ち、関係のないリファクタリングを混ぜない。

## 実装開始前の必須PoCゲート

- `docs/POC_GATE.md`を最優先で実施する。
- `docs/POC_RESULT.md`の結論が`GO`になるまで、`cmd/`、`internal/`、`go.mod`など製品コードを作成しない。
- ヘルプ表示、仕様書確認、机上調査だけでPoC完了としない。短い検証用MKVを実際に生成または用意し、HandBrakeCLIで実エンコードして結果を機械的に検証する。
- PoC用の一時スクリプトや最小Goスパイクは`poc/`または一時ディレクトリへ隔離し、製品アーキテクチャへ流用しない。
- 必須項目が一つでも未確認・失敗なら`NO-GO`または`GO WITH CHANGES`とし、設計修正後に影響範囲のPoCを再実行する。
- 実装を始めてから根本的なHandBrakeCLI制約へ気づく状態を避けることを最優先とする。

## Go実装方針

- Go 1.26系を対象とし、利用可能な最新パッチ版で検証する。
- Go標準ライブラリを優先し、外部依存はTUIなど明確な価値があるものだけに限定する。
- `gofmt`準拠。Goらしい単純な制御フロー、早期return、小さな関数を使う。
- パッケージ名は短く具体的にする。`util`、`common`、`helpers`へ無関係な処理を集めない。
- 公開APIは最小限にする。アプリ内部のコードは原則`internal/`以下へ置く。
- インターフェースは利用側で定義し、テストや境界分離に必要な最小メソッドだけを持たせる。全構造体に機械的なインターフェースを作らない。
- 不要なDIコンテナ、サービスロケータ、ジェネリクス、抽象ファクトリ、イベントバスを導入しない。
- `context.Context`は外部コマンド実行、キャンセル可能な処理、アプリケーション境界で伝播させる。構造体フィールドへ保存しない。
- エラーは意味を付けて`%w`でラップする。通常の実行エラーで`panic`しない。
- ログには`log/slog`を使用する。利用者向け表示と診断ログを混同しない。
- 外部コマンドはシェル文字列として組み立てず、`os/exec`へ引数配列で渡す。
- パスは`path/filepath`で扱い、macOSの空白・日本語を含むパスを正しく処理する。
- JSONは型付き構造体で読み書きし、読み込み時に明示的に検証する。
- `queue.json`と`settings.json`の書き込みは同一ディレクトリ内の一時ファイルへ完全に書き、`fsync`相当を検討したうえで原子的に置換する。
- キュー破損時はfail closedとし、既存ファイルを推測で修復・上書きしない。
- テストでは実HandBrakeCLI、ffmpeg、mkvpropeditに依存しない層を厚くする。コマンド実行境界を差し替え可能にする。
- テーブル駆動テストを基本にし、ファイル操作は`testing.T.TempDir()`を使う。

## 必須検証

各マイルストーン完了時に最低限次を実行する。

```sh
gofmt -w .
go test ./...
go test -race ./...
go vet ./...
go build ./...
```

導入した外部リンターがある場合は、それも実行する。ただし、リンター導入自体を目的化しない。

## 絶対条件

- エンコード範囲はHandBrakeCLIのチャプター番号範囲で指定する。秒・ミリ秒・PTSによる切り出しへ変更しない。
- 字幕の焼き付けは実装しない。プリセット由来の自動焼き付けも明示的に無効化する。
- キュー先頭ジョブは成功するまで`queue.json`から削除しない。
- 即時中断時はHandBrakeCLIを停止し、処理中の出力ファイルを削除し、先頭ジョブを残す。
- 実行時の既存出力は、キュー追加時に承認済みとして確認なしで削除・上書きする。
- HandBrakeCLIは最終出力名へ直接書かず、出力ディレクトリ内のジョブ固有一時ファイルへ書く。
- DLNA表示用タイトルは最終出力ファイル名から拡張子を除いた値に固定する。
- MKVは`mkvpropedit`でタイトルだけを直接変更し、FFmpegによる再多重化を行わない。
- MP4はFFmpegのストリームコピーでタイトルを設定し、再エンコードしない。
- タイトル設定と検証が完了するまで最終ファイル名へ公開しない。
- 手作業によるJSON編集は裏技的な復旧手段であり、不正な手編集を自動修復する責任をアプリへ持たせない。
